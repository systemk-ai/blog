---
title: "Hello World"
description: "The first article — a tour of math, code, and headings."
tags: ["intro", "meta"]
pubDate: 2026-06-25
draft: false
---

Welcome to the blog. This article exercises the rendering pipeline.

## Math

Inline math like $E = mc^2$ and a display block:

$$
\int_0^\infty e^{-x}\,dx = 1
$$

## Code

```ts
export function greet(name: string): string {
	return `Hello, ${name}!`;
}
```

## Headings get anchors

Each heading above is auto-linked via `rehype-slug` + `rehype-autolink-headings`.
